# W1D1 · Fri 7 Aug 2026 · 11:00–14:00 IST

**Topic:** Environment, Python execution model, variables, types, operators, REPL vs script
**Status:** complete · **Quiz 1: 57/100** · commit `f7e9b0f`

## Covered
- Names as bindings to objects; dynamic vs strong typing
- **`=` rebinds, methods mutate** — the day's core rule
- Mutable (list/dict/set) vs immutable (int/float/str/bool/tuple)
- Pass by assignment; no `ref` in Python
- Types: arbitrary-precision `int`, `float` (IEEE-754), `bool` as `int` subclass, `None` as singleton
- Operators: `/` always float, `//` floors toward −∞, `%` sign follows divisor, `**`, chained comparison, `and`/`or` return operands, truthiness
- Float precision: `0.1 + 0.2 != 0.3`

## Quiz breakdown
| Section | Score |
|---|---|
| MCQ | 30/30 |
| Short answer | 15/36 (Q7 = 0, unattempted) |
| Code debug | 12/14 |
| Applied | 0/20 (unattempted) |

## Confusion list (feeds Exam 1, Sat 15 Aug)
1. Rebinding a parameter inside a function vs mutating it — **Q7, unattempted despite being taught**
2. Designing for immutability + the memory tradeoff — Q10, unattempted
3. `is` vs `==` for `None`; None as a singleton (answer was shallow)
4. Floor division wording: "moves left on the number line", not "bigger number"
5. `or` returns an *operand*, not a boolean
6. Binary float representation — mechanism, not the 1/3 analogy

## Tutor note
Recognition strong (30/30 MCQ), generation avoided. Four separate instances today of
answering the first half of a question and stopping. This is the primary risk to Phase 2.

## Homework before Mon 10 Aug
Re-answer Q7 and Q10 in own words, committed to `notes/`.
