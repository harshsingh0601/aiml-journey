# Daily check · D1 · Running Python

**Tue 11 Aug 2026 · 5 questions · /50 · asked one at a time**

## Score: 47/50

| Q | Type | Marks | Awarded |
|---|---|---|---|
| 1 | Recall — the two passes | 8 | **5** |
| 2 | Recall — quotes vs no quotes | 8 | **8** |
| 3 | Trace — comment + arithmetic | 10 | **10** |
| 4 | Trace — SyntaxError | 10 | **10** |
| 5 | Write — four-line file | 14 | **14** |
| | | **50** | **47** |

**Band: 40–50 — landed.** Next concept tomorrow. `python` axis +1.0.

---

## Q1 · Recall · 5/8

> When you run `uv run python myfile.py`, Python does two things, in order, before your output appears. What are they, and what's the difference?

**His answer:** "Python first checks the file for the SyntaxErrors and then goes line by line for NameErrors before the output appears. SyntaxErrors are errors occurred when a correct syntax is missed to be used like brackets, double quotes, single quotes etc. NameErrors are errors when a mistake occurs in the naming of functions, keywords etc."

**Marking.** Two-pass structure present and correctly ordered — that carries most of the marks. One real conceptual slip: he described pass 2 as *"goes line by line for NameErrors"*, i.e. as a second **checking** pass. Pass 2 is **execution**. `NameError` is not what pass 2 is looking for; it is one thing that can go wrong while pass 2 does the actual work.

That distinction is not pedantry — it is the whole explanation for his own Experiment B, where lines 1–3 *ran and printed* rather than being checked and passed. Docked 3.

**Correct answer.** Pass 1 reads the entire file and checks it is grammatically valid Python. Nothing executes. Failure here → `SyntaxError`, and not a single line runs. Pass 2, only if pass 1 succeeded, executes the file top to bottom. Failure here stops execution *at that line*, and everything above it has already happened irreversibly.

**Noted, not penalised:** he attributed `NameError` to misspelled *keywords*. Misspelling a real keyword generally produces a `SyntaxError`. Flagged for D2, where names are the whole topic.

---

## Q2 · Recall · 8/8

> Difference between `print(2 + 2)` and `print("2 + 2")`, and what does each show?

**His answer:** Unquoted → Python treats as numbers and performs the addition. Quoted → string, printed as-is. Outputs `4` and `2 + 2`.

**Marking.** Correct on both output and mechanism. Wording slightly loose ("resemble strings"); marked generously per rubric — the understanding is unambiguous. Full marks.

---

## Q3 · Trace · 10/10

```python
print("A")
# print("B")
print("C")
print(3 + 4)
```

**His answer:** `A` / `C` / `7`

**Marking.** Correct. Skipped the commented line, preserved order, let Python evaluate the arithmetic instead of echoing it. Full marks.

---

## Q4 · Trace · 10/10

```python
print("start")
print("middle")
print("end"
```

**His answer:** "SyntaxError"

**Marking.** Correct. The question asked what appears on screen; listing no output *is* the right answer, and the rubric forbids docking for terseness. Corroborated independently by his recap answer to Scenario 2 ("No nothing run as first pass only checks the file syntactically"), so this is understood rather than guessed.

**Significant:** this is the exact case he predicted wrong in Experiment A earlier the same morning. Retested and correct. Full marks.

---

## Q5 · Write · 14/14

> Four lines: a greeting, a comment Python ignores, the result of 25 + 17 calculated by Python, a goodbye. Plus the exact output.

**His code:**

```python
print("Hello Claude, Thanks for being my tutor")
# print("Example of a comment, I understand this line will be missed and treated as a comment because of # symbol")
print(25 + 17)
print("goodbye")
```

**His output:**

```
Hello Claude, Thanks for being my tutor
42
goodbye
```

**Marking.** All four requirements met. `print(25 + 17)` correctly unquoted so Python evaluates rather than echoes. Output exact, comment correctly absent. Written before running, as instructed.

Initially submitted code without the output; prompted once for the missing half and supplied it correctly. Not penalised — the omission was of the answer's second half, not of understanding. Full marks.

---

## Tutor's read

The score is real but the more informative event happened in the recap, not the check.

Asked to explain `SyntaxError` vs `NameError`, he first returned **definitions** when the question asked for **consequences**. Re-asked, he returned my own sentences verbatim — the concept note for one answer, my previous message for the other. That is the precise gap the recap block exists to catch, and it caught it.

Re-testing with three unseen application scenarios resolved it: all three correct, reasoned from the two-pass model, and phrased in his own language ("it got errored on the check 1"). The understanding is genuine. It simply needed a test that could not be answered by recall.

**Method note for future sessions: when an answer comes back in my phrasing, do not accept it. Re-test with an unseen scenario.** This worked and should be standard.

The strongest moment of the day was Experiments A and B, where he predicted both outcomes and got both inverted. Being wrong in both directions is what made the compile/run distinction stick — by Q4 he had it cold. Worth repeating the pattern: predict first, then run, especially where the prediction is likely wrong.

**Weakest point:** pass 2 framed as checking rather than executing (Q1). Costs nothing today; matters from D2 onward when variables start holding state that execution actually changes. **D2 opens with a five-minute correction of this, then proceeds.**
